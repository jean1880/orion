'use strict';

/**
 * @ngdoc function
 * @name dogToolApp.controller:PeopleNewCtrl
 * @description
 * # PeopleNewCtrl
 * Controller of the dogToolApp
 */
angular.module('dogToolApp')
  .controller('PeopleNewCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
